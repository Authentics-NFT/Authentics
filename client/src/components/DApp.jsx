import {
    Grid,
    Typography,
} from "@mui/material";

function DApp({contract, accounts, dispatch}) {
    return <Grid container alignItems={'center'} justifyContent={'center'}>
       <Typography>Authentics.</Typography>
    </Grid>
}

export default DApp;
